# Dependencies
node_modules/

# Build output
# NOTE: client/dist is intentionally committed as a prebuilt deploy artifact.
# GoDaddy PaaS cannot build the Angular bundle (its sandbox can't execute
# esbuild), so the site is built locally and the output is deployed as-is.
# See scripts/build.mjs and CLAUDE.md §9.
server/dist/
**/.angular/

# Environment
.env
.env.local
.env.*.local

# Passenger
tmp/

# Logs
*.log
npm-debug.log*
logs/

# Coverage
coverage/
.nyc_output/

# Editor / OS
.vscode/
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Optional caches
.cache/
.parcel-cache/
.eslintcache
