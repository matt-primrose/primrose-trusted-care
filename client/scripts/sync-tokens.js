// TODO (v2): generate client/src/styles/_tokens.scss from brand-assets/palette.json so
// the two never drift. For v1, _tokens.scss is hand-maintained — when palette.json changes,
// update _tokens.scss in the same PR.
//
// When this script is implemented, wire it into npm prebuild so the SCSS is regenerated
// before every build.

console.log('sync-tokens.js: not yet implemented. Edit client/src/styles/_tokens.scss by hand.');
